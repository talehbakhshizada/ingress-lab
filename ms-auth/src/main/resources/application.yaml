logging:
  level:
    org:
    springframework:
      security: DEBUG
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: VtbBank123!
  jpa:
    hibernate:
      ddl-auto: update